@page "/criminal/diversion-plea"
@page "/criminal/diversion-plea/{CaseNumber}"
@inherits FormPageBase<DiversionPleaDto>

<div class="form-page">
<div class="form-card">
<h2>Diversion Dialog</h2>

<EditForm Model="Model" OnValidSubmit="HandleValidSubmit">
    <FluentValidationValidator />
    @if (IsLoadingCase)
    {
        <div class="alert alert-info">Loading case data...</div>
    }
    <ValidationSummary />
    <div class="form-group">
        <label>Defendant First Name</label>
        <InputText @bind-Value="Model.DefendantFirstName" class="form-control" />
    </div>
    <div class="form-group">
        <label>Defendant Last Name</label>
        <InputText @bind-Value="Model.DefendantLastName" class="form-control" />
    </div>
    <div class="form-group">
        <label>Case Number</label>
        <InputText @bind-Value="Model.CaseNumber" class="form-control" />
    </div>
    <div class="form-group">
        <label>Date</label>
        <InputDate @bind-Value="Model.Date" class="form-control" />
    </div>
    <div class="form-group">
        <label>Defense Counsel Name</label>
        <InputText @bind-Value="Model.DefenseCounselName" class="form-control" />
    </div>
    <div class="form-group">
        <label>Defense Counsel Type</label>
        <InputText @bind-Value="Model.DefenseCounselType" class="form-control" />
    </div>
    <div class="form-group">
        <label>Defense Counsel Waived</label>
        <InputCheckbox @bind-Value="Model.DefenseCounselWaived" />
    </div>
    <div class="form-group">
        <label>Appearance Reason</label>
        <InputText @bind-Value="Model.AppearanceReason" class="form-control" />
    </div>
    <div class="form-group">
        <label>Charges</label>
        <InputText @bind-Value="Model.Charges" class="form-control" />
    </div>
    <div class="form-group">
        <label>Diversion Type</label>
        <InputText @bind-Value="Model.DiversionType" class="form-control" />
    </div>
    <div class="form-group">
        <label>Diversion Completion Date</label>
        <InputDate @bind-Value="Model.DiversionCompletionDate" class="form-control" />
    </div>
    <div class="form-group">
        <label>Diversion Fine Pay Date</label>
        <InputDate @bind-Value="Model.DiversionFinePayDate" class="form-control" />
    </div>
    <div class="form-group">
        <label>Probation</label>
        <InputCheckbox @bind-Value="Model.Probation" />
    </div>
    <div class="form-group">
        <label>Pay Restitution</label>
        <InputCheckbox @bind-Value="Model.PayRestitution" />
    </div>
    <div class="form-group">
        <label>Pay Restitution To</label>
        <InputText @bind-Value="Model.PayRestitutionTo" class="form-control" />
    </div>
    <div class="form-group">
        <label>Pay Restitution Amount</label>
        <InputNumber @bind-Value="Model.PayRestitutionAmount" class="form-control" />
    </div>
    <div class="form-group">
        <label>Other Conditions</label>
        <InputCheckbox @bind-Value="Model.OtherConditions" />
    </div>
    <div class="form-group">
        <label>Other Conditions Text</label>
        <InputText @bind-Value="Model.OtherConditionsText" class="form-control" />
    </div>
    <div class="form-actions">
        <button type="submit" class="btn btn-success" disabled="@(IsSubmitting || IsLoadingCase)">Generate DOCX</button>
        <button type="button" class="btn btn-danger" @onclick="CloseOrCancel">Close Dialog</button>
        <button type="button" class="btn btn-secondary" @onclick="CloseOrCancel">Cancel</button>
        <button type="button" class="btn btn-warning" @onclick="ClearFields">Clear Fields</button>
    </div>
</EditForm>

@if (SubmitResult != null)
{
    <div class="alert alert-success">@SubmitResult</div>
}
@if (ErrorMessage != null)
{
    <div class="alert alert-danger">@ErrorMessage</div>
}

</div>
</div>

@code {

    protected override string ApiEndpoint => "diversionplea";

    protected override Task PopulateFromCaseAsync(List<CaseSearchResultDto> results)
    {
        var first = results[0];
        Model.CaseNumber = CaseNumber;
        Model.DefendantFirstName = first.DefFirstName;
        Model.DefendantLastName = first.DefLastName;
        Model.DefenseCounselName = first.DefenseCounsel;
        Model.DefenseCounselType = first.PubDef ? "Public Defender" : "Private Counsel";
        Model.Charges = string.Join("; ", results
            .Where(r => r.Charge != null)
            .Select(r => $"{r.Charge} ({r.Statute}) - {r.DegreeCode}"));
        return Task.CompletedTask;
    }
}

{
  "ApiBaseUrl": "http://api/api/",

  // ENTRA ID - Override Authority/ClientId here if Docker needs different values than appsettings.json.
  "AzureAd": {
    "Authority": "https://login.microsoftonline.com/REPLACE_WITH_TENANT_ID",
    "ClientId": "REPLACE_WITH_CLIENT_ID",
    "ValidateAuthority": true
  }
}
